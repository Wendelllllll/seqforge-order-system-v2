# SeqForge Order System V2 Demo：技术原理、建立过程与销售讲解手册

更新时间：2026 年 9 月 7 日（更新跨平台安装步骤）

适用对象：SeqForge 管理层、销售人员、实验室运营人员、潜在合作伙伴、后续开发人员

---

## 一、先用一句话理解这个 Demo

SeqForge Order System V2 是一个可以在本地电脑真实运行的 Sanger 测序订购系统原型。客户可以注册、登录、创建包含多个样本和引物的订单；管理员可以收到订单、查看客户和样本信息、更新实验室处理状态、上传结果；最后客户可以在自己的账户里看到完成状态并下载结果。

它不是一组只能点击的静态设计图，而是一条真正贯通了页面、账户、数据库、权限、订单和结果文件的完整业务流程。

最核心的闭环是：

```text
客户注册
  ↓
客户登录
  ↓
创建 Sanger 订单并添加多个样本
  ↓
订单保存到数据库
  ↓
管理员看到新订单
  ↓
管理员更新实验室状态
  ↓
客户看到最新状态
  ↓
管理员上传测序结果
  ↓
客户下载属于自己的结果
```

这条闭环证明了一件重要的事：SeqForge 不需要继续被旧 Retrogen 订购系统的技术债务绑住，可以在新的架构上重新建立客户体验和实验室工作流。

---

## 二、为什么先做这个 Demo

### 2.1 它要解决的现实问题

旧订购系统存在稳定性差、代码老旧、注册受 reCAPTCHA 问题影响等情况。如果直接修补旧系统，团队会不断把时间投入到已经不适合未来业务的架构里。

V2 Demo 采取的是“重新建立最小业务闭环”的方法：

1. 先证明新客户可以注册和登录。
2. 再证明真实订单可以被创建和保存。
3. 再证明客户数据和管理员操作可以通过权限隔离。
4. 最后证明结果能够回到正确客户手里。

只要这条链路成立，后续增加 96 孔板、CSV 导入、客户价格、上门取样、Oligo、Fragment Analysis、ABI 3730 和 LIMS 对接，就有了可靠的基础。

### 2.2 为什么第一版只做 Sanger

第一版故意不把所有服务一起放进去，因为产品开发最危险的做法之一，是在最基本流程还没有验证前就堆积大量功能。

Sanger 是一个非常合适的第一条业务线：

- 订单信息容易理解；
- 一个订单可以包含多条测序反应；
- 每条反应都有样本、模板、浓度、引物和备注；
- 实验室处理状态具有清晰的先后顺序；
- 完成后有明确的结果文件交付动作。

因此，它足以验证整个系统的核心能力，同时又不会让原型变得过度复杂。

### 2.3 Demo 的准确定位

目前应该把它称为：

> Functional local prototype，功能性本地原型。

可以说：

- 核心客户到管理员流程已经真实运行；
- 数据会保存，不会因为页面刷新而消失；
- 客户和管理员具有不同权限；
- 订单状态和结果文件会真实同步；
- 已经验证新系统的技术方向可行。

不应该说：

- 已经是生产系统；
- 已经可以直接接收真实客户数据；
- 已经完成所有安全、合规和灾难恢复工作；
- 已经具备复杂组织权限、价格引擎或仪器自动化；
- 已经替代了现有生产网站。

---

## 三、产品中有哪两类用户

## 3.1 客户用户

目标用户包括大学实验室、研究机构、生物科技公司、制药公司、研究员和实验室经理。

客户可以：

- 注册账户；
- 使用邮箱和密码登录；
- 查看自己的客户仪表盘；
- 创建新的 Sanger 测序订单；
- 动态添加多个样本；
- 为每个样本填写模板、浓度、引物和引物来源；
- 提交订单并获得 SeqForge 订单编号；
- 查看自己的历史订单；
- 查看订单当前实验室状态；
- 在结果上传后下载文件；
- 查看账户关联的机构和实验室资料。

客户不能：

- 查看其他客户的订单；
- 进入管理员页面；
- 修改实验室状态；
- 为订单上传结果。

## 3.2 管理员用户

管理员代表 SeqForge 的运营和实验室团队。

管理员可以：

- 查看所有客户提交的订单；
- 查看客户姓名、邮箱、机构、实验室和电话；
- 查看订单里的所有样本和引物；
- 查看 PO 号码和特别说明；
- 将状态修改为 Submitted、Received、Processing、Sequenced、QC 或 Completed；
- 查看状态变化历史；
- 上传结果文件；
- 下载当前结果文件进行核对。

当管理员上传结果时，系统会自动把订单设为 Completed，并把完成事件写入状态历史。

---

## 四、系统总体架构

可以把整个系统理解成五层：

```text
第一层：浏览器页面
客户页面、管理员页面、表单、状态显示、结果下载
                    ↓
第二层：Next.js 应用
页面路由、服务器组件、API Route Handlers、页面跳转
                    ↓
第三层：业务与安全
Better Auth 登录会话、角色判断、Zod 输入验证、订单规则
                    ↓
第四层：数据访问
Prisma ORM，把 TypeScript 业务代码转换为数据库操作
                    ↓
第五层：本地存储
SQLite 数据库 + storage/results 结果文件目录
```

这是一种“单体全栈应用”架构。前端页面和后端接口都在同一个 Next.js 项目里，适合快速建立原型，也减少了前后端分开部署带来的复杂度。

### 4.1 一次页面请求发生了什么

以客户打开订单详情页为例：

1. 浏览器请求 `/orders/某个订单ID`。
2. Next.js 在服务器端读取当前登录会话。
3. 系统判断用户是不是客户。
4. 系统使用当前用户 ID 和订单 ID 一起查询数据库。
5. 如果订单属于该用户，服务器读取订单、样本、结果和状态历史。
6. Next.js 生成页面并返回浏览器。
7. 如果订单不属于该用户，则返回 404，不泄露订单内容。

这里的重点是：权限判断并不只存在于页面按钮上，而是在服务器查询数据时再次执行。隐藏按钮只能改善界面，服务器验证才是真正的安全边界。

---

## 五、使用了哪些技术和工具

| 工具 | 当前版本 | 在项目中的作用 | 非技术人员的理解方式 |
| --- | --- | --- | --- |
| Node.js | 推荐 22 LTS | 在本地运行服务器和开发工具 | 整个网站运行所需的基础引擎 |
| npm | 随 Node.js 安装 | 下载和管理项目依赖 | 软件零件仓库和安装工具 |
| Next.js | 16.3.4 | 页面、路由、服务器逻辑和 API | 网站的主体框架 |
| React | 19.2.8 | 创建可交互页面组件 | 负责表单、按钮和动态页面体验 |
| TypeScript | 5.x | 给 JavaScript 增加类型检查 | 在运行前发现很多字段和数据错误 |
| Tailwind CSS | 4.x | 构建页面布局、颜色和响应式设计 | 快速建立统一、专业的界面 |
| Better Auth | 1.7.2 | 注册、登录、密码处理和会话 | 专业账户与身份验证模块 |
| Prisma ORM | 6.19.3 | 定义数据结构并读写数据库 | TypeScript 与数据库之间的翻译层 |
| SQLite | 本地文件数据库 | 保存客户、订单、样本和状态 | 不需要购买或安装数据库服务器 |
| Zod | 4.1.11 | 在服务器验证订单输入 | 防止空值、超长值和错误格式进入数据库 |
| Lucide React | 0.544.0 | 页面图标 | 统一的界面图标库 |
| ESLint | 9.x | 检查代码质量和常见错误 | 自动代码审查工具 |
| Webpack | Next.js 内置 | 编译开发版和生产构建 | 把源代码打包成浏览器和服务器可运行的程序 |
| Git | 本地仓库 | 追踪源代码变化 | 项目的版本历史和恢复机制 |

### 5.1 为什么使用 Next.js

Next.js 同时处理浏览器页面和服务器端逻辑，非常适合这个项目：

- 页面和 API 在同一个代码库里；
- 可以在服务器上直接读取数据库；
- 可以在返回页面前先验证登录状态；
- 动态路由很适合 `/orders/[id]` 这种订单详情页；
- 后续可以迁移到正式部署环境；
- TypeScript 支持完整，便于维护。

### 5.2 为什么使用 TypeScript

生物实验室订购系统包含很多有明确含义的数据，例如订单状态、样本字段、客户角色和结果文件。

TypeScript 可以在开发阶段检查：

- 某个订单字段是否存在；
- 页面收到的数据类型是否正确；
- API 是否可能传入错误状态；
- 数据库返回值是否被正确处理；
- 函数调用是否缺少必要参数。

它不能代替所有测试，但能大幅减少因为拼写或字段理解错误造成的问题。

### 5.3 为什么使用 Tailwind CSS

Tailwind CSS 让界面样式直接与组件一起维护。这个 Demo 没有采用夸张渐变、巨大留白和过多圆角，而是采用实验室运营系统更合适的视觉语言：

- 深色导航；
- 高对比度数据表；
- 清晰的状态标签；
- 紧凑但不拥挤的表单；
- 桌面与较小屏幕都可以使用；
- 青色作为与生物科技相关的强调色；
- 明确区分客户入口和管理员入口。

### 5.4 为什么使用 Better Auth

账户系统不是简单地“把密码写进数据库”。它需要密码哈希、会话 token、登录 cookie、登录状态读取和账户关系管理。

本项目没有自己发明一套不安全的登录算法，而是使用 Better Auth。当前 Demo 中：

- 密码不会以明文存储；
- 密码由认证库进行 scrypt 哈希；
- 登录后创建独立会话；
- 会话 token 在数据库中有唯一约束；
- `customer` 和 `admin` 角色被分开；
- 新用户不能通过注册表单把自己设置成管理员；
- 本地可信来源限制为 `http://localhost:3000`。

### 5.5 为什么使用 Prisma

Prisma 是应用和数据库之间的 ORM，也就是对象关系映射工具。

开发人员不需要在每个页面里手写原始 SQL，而是可以写带类型的查询，例如：

```ts
prisma.order.findMany({
  where: { userId: currentUser.id },
  include: { samples: true, result: true },
});
```

它表达的含义是：“读取当前用户的订单，同时读取订单的样本和结果。”

Prisma 的价值包括：

- 数据模型集中定义；
- 自动生成 TypeScript 类型；
- 数据库迁移可以保存结构变化历史；
- 查询关系更清楚；
- 原型使用 SQLite，生产阶段可以规划迁移到 PostgreSQL。

### 5.6 为什么 Demo 使用 SQLite

SQLite 把整个数据库保存在一个本地文件 `prisma/dev.db` 中。

它的优点是：

- 成本为零；
- 不需要安装独立数据库服务器；
- 适合本地演示和快速开发；
- 数据在页面刷新和服务器重启后仍然存在；
- 可以方便地复制或重建演示环境。

它不代表生产环境也应该永远使用 SQLite。正式多用户生产系统更适合 PostgreSQL，因为 PostgreSQL 在并发、备份、权限、可用性和运营工具方面更成熟。

### 5.7 为什么使用 Zod

浏览器里的 `required` 只能帮助正常用户填写表单，不能被当成服务器安全规则。任何人都可能绕过页面，直接向 API 发送数据。

因此 `/api/orders` 会在服务器使用 Zod 再检查一次：

- 订单名不能为空并且最多 120 个字符；
- PO 最多 80 个字符；
- 特别说明最多 2,000 个字符；
- 每个订单至少一个样本，最多 200 个样本；
- 样本名和引物不能为空；
- 每个字段都有长度限制。

这叫作 server-side validation，也就是服务器端验证。

---

## 六、项目是怎样一步一步建立起来的

### 第一步：把业务需求缩小为一条纵向流程

没有先做 Oligo、Fragment Analysis、付款、报价、ABI 自动化或复杂价格，而是先确定最小闭环：

```text
账户 → Sanger 订单 → 样本 → 管理员状态 → 结果文件
```

这叫 vertical slice。它不是只完成某一层，而是从界面一直贯通到数据库和结果交付。

### 第二步：初始化应用骨架

使用官方 Next.js 工具创建了：

- App Router 项目；
- TypeScript；
- Tailwind CSS；
- ESLint；
- `src/` 目录结构；
- npm 依赖管理。

### 第三步：建立数据库模型

在 `prisma/schema.prisma` 中定义账户、会话、订单、样本、结果和状态历史，并使用 Prisma migration 建立 SQLite 表。

数据库结构不是页面临时数据。客户提交后，订单会真正写入 SQLite。

### 第四步：建立注册、登录和角色

Better Auth 负责：

- 注册；
- 邮箱密码登录；
- 密码哈希；
- 登录会话；
- 登出；
- 用户自定义资料字段。

系统定义两个角色：

- `customer`
- `admin`

角色字段在注册接口中被设置为不可输入，因此普通注册用户不能提交 `role: admin` 来提升权限。

### 第五步：建立客户页面

客户侧包括：

- 首页；
- 注册页；
- 登录页；
- 仪表盘；
- 订单历史；
- 新订单页；
- 订单详情页；
- 账户资料页；
- 结果下载入口。

### 第六步：建立动态样本表单

新订单页面由 React Client Component 管理样本数组。用户点击 “Add another sample” 时，会新增一个样本对象和一组表单字段。

每个样本包含：

- Sample name
- Template type
- Concentration
- Primer
- Primer source
- Notes

提交时，整个订单和全部样本以 JSON 发送到服务器。

### 第七步：建立订单 API

服务器执行以下动作：

1. 读取登录会话；
2. 确认请求来自客户而不是管理员；
3. 使用 Zod 验证数据；
4. 生成订单编号；
5. 创建订单；
6. 同时创建所有样本；
7. 写入第一条 Submitted 状态历史；
8. 返回新订单 ID 和订单编号。

### 第八步：建立管理员工作流

管理员页面会读取所有订单并显示：

- 订单编号和名称；
- 客户；
- 机构和实验室；
- 服务类型；
- 样本数量；
- 提交日期；
- 当前状态；
- 结果是否已经上传。

管理员进入详情后可以更新状态，也可以上传结果。

### 第九步：建立结果文件保护

上传的文件不会直接放在公开网页目录里，而是写入：

```text
storage/results/
```

数据库只保存文件的显示名、内部随机名、类型、大小和上传时间。

客户下载时必须通过受保护的 API。服务器先确认当前用户是管理员或订单所有者，然后才读取文件并返回。

### 第十步：验证完整流程

项目完成后执行了：

- ESLint 代码检查；
- TypeScript 类型检查；
- Next.js production build；
- npm 依赖安全审计；
- 浏览器注册测试；
- 客户登录测试；
- 两个样本的订单提交测试；
- 管理员状态更新测试；
- 管理员结果上传测试；
- 客户结果下载测试；
- 不同客户之间的订单隔离测试；
- 响应式界面检查。

---

## 七、项目目录结构

```text
SeqForge New Order System/
├── prisma/
│   ├── schema.prisma          数据库模型
│   ├── migrations/            数据库结构变更历史
│   ├── seed.ts                创建本地演示账户
│   └── dev.db                 SQLite 数据库，不提交到 Git
├── storage/results/           管理员上传的结果文件
├── src/
│   ├── app/
│   │   ├── (customer)/        客户受保护页面
│   │   ├── (admin)/admin/     管理员受保护页面
│   │   ├── api/               订单、状态、结果和认证接口
│   │   ├── login/             登录页
│   │   ├── register/          注册页
│   │   ├── page.tsx           公共首页
│   │   └── globals.css        全局视觉样式
│   ├── components/            可复用页面组件
│   └── lib/                   认证、数据库、状态和会话工具
├── .env                       本地密钥和数据库地址，不提交到 Git
├── .env.example               环境配置示例
├── package.json               依赖和开发命令
├── prisma.config.ts           Prisma 配置
├── README.md                  本地运行说明
└── PROJECT_PROGRESS.md        当前进度清单
```

括号目录 `(customer)` 和 `(admin)` 是 Next.js Route Group。它们帮助代码分类，但不会出现在网址中。

例如：

```text
src/app/(customer)/orders/page.tsx
```

对应的网址仍然是：

```text
/orders
```

---

## 八、数据库详细说明

### 8.1 数据关系总图

```text
User（用户）
 ├── 1 对多 Session（登录会话）
 ├── 1 对多 Account（登录方式）
 └── 1 对多 Order（订单）
                    ├── 1 对多 Sample（样本/测序反应）
                    ├── 1 对多 StatusHistory（状态历史）
                    └── 0 或 1 个 Result（结果文件）

Verification（验证记录）属于认证基础设施
```

### 8.2 User：用户表

主要字段：

| 字段 | 含义 |
| --- | --- |
| `id` | 用户唯一 ID |
| `name` | 完整姓名 |
| `email` | 登录邮箱，必须唯一 |
| `emailVerified` | 邮箱是否验证 |
| `role` | `customer` 或 `admin` |
| `firstName` | 名 |
| `lastName` | 姓 |
| `organization` | 机构或公司 |
| `labName` | 实验室名称 |
| `phone` | 可选电话 |
| `createdAt` | 创建时间 |
| `updatedAt` | 更新时间 |

当前 Demo 为了速度，把 organization 和 labName 直接保存在用户资料里。这足以完成单客户、单实验室的原型流程。

生产版应该逐步拆成：

```text
Organization
  └── Lab
       └── Membership / User
```

拆分后才能更好地支持：

- 一个机构下多个实验室；
- 一个实验室多个成员；
- PI、Lab Manager、Researcher 等不同角色；
- 实验室级价格；
- 离职或成员变更；
- 共享订单和结果。

### 8.3 Session：登录会话表

Session 代表一次有效登录。主要内容包括：

- 唯一会话 token；
- 对应用户 ID；
- 到期时间；
- IP 地址和浏览器信息；
- 创建和更新时间。

用户登出后，相关会话失效。受保护页面通过会话判断是谁正在访问。

### 8.4 Account：认证账户表

Account 保存用户使用哪一种方式登录。目前主要是邮箱密码 credential 账户。

密码哈希由认证库保存在这个表里，而不是放在页面或浏览器代码中。`issuer + accountId` 具有联合唯一约束，避免相同身份被重复关联。

未来如果增加 Google、Microsoft 或机构 SSO，同一个 User 可以关联多个 Account。

### 8.5 Verification：验证表

Verification 是认证系统预留的验证记录表，可支持有期限的验证 token。当前本地 Demo 没有发送真实邮箱验证或密码重置邮件，但认证结构已经包含所需的基础表。

### 8.6 Order：订单表

Order 是业务中心。

| 字段 | 含义 |
| --- | --- |
| `id` | 内部订单 ID |
| `orderNumber` | 客户可见的 SeqForge 订单号 |
| `userId` | 下单客户 |
| `service` | 服务类型，目前固定为 `SANGER` |
| `orderName` | 客户项目名或参考名 |
| `poNumber` | 可选 PO |
| `specialInstructions` | 可选特别说明 |
| `status` | 当前实验室状态 |
| `createdAt` | 提交时间 |
| `updatedAt` | 最后更新时间 |
| `completedAt` | 完成时间 |

`service` 字段虽然目前只有 SANGER，但它没有把整个数据库表命名成 SangerOrder。这是一个重要的扩展设计：未来其他服务仍然可以进入通用订单体系，再用不同的 order item 或 service-specific data 扩展。

### 8.7 Sample：样本和测序反应表

一个 Order 可以包含多个 Sample。

| 字段 | 含义 |
| --- | --- |
| `orderId` | 所属订单 |
| `position` | 样本在订单中的顺序 |
| `sampleName` | 客户样本名 |
| `templateType` | Plasmid、PCR product 等 |
| `concentration` | 可选浓度和单位 |
| `primerName` | 引物名称 |
| `primerSource` | 客户提供、通用引物或合成引物 |
| `notes` | 样本级备注 |

同一订单中的 position 不能重复，数据库通过联合唯一约束保证样本顺序的完整性。

### 8.8 Result：结果表

一个订单当前最多有一个 Result。

Result 记录：

- 客户看到的原始文件名；
- 磁盘上的随机内部文件名；
- MIME 类型；
- 文件大小；
- 上传时间；
- 所属订单。

随机内部名可以避免两个客户上传同名文件时互相覆盖，也降低通过猜文件名直接访问文件的风险。

未来生产版可以把一对一改成一对多，以支持同一订单的多个 AB1、FASTA、PDF QC 报告和 ZIP 文件。

### 8.9 StatusHistory：状态历史表

Order 表只保存当前状态，StatusHistory 保存每一次状态变化。

这样可以回答：

- 订单什么时候提交；
- 什么时候进入 Processing；
- 什么时候完成；
- 状态是否发生过重复或回退。

生产版还可以增加 `changedByUserId`、操作原因、实验室工位和备注，形成更完整的审计日志。

### 8.10 数据库索引和约束

项目已经对常用查询增加索引：

- User email 唯一；
- Session token 唯一；
- Order orderNumber 唯一；
- 按 userId 和 createdAt 查询客户订单；
- 按 status 和 createdAt 查询管理员队列；
- 按 orderId 查询样本和状态历史；
- 一个订单最多关联一个结果。

索引的商业意义是：随着订单增加，常用列表不需要每次扫描全部数据。

---

## 九、订单编号如何生成

订单编号格式为：

```text
SF-YYMM####
```

示例：

```text
SF-26090001
```

含义是：

- `SF`：SeqForge；
- `26`：2026 年；
- `09`：9 月；
- `0001`：当月顺序号。

Demo 会读取当月最大的订单编号，再加一生成新编号。

这个方法适合本地单服务器原型。生产环境有多个用户同时下单时，应该改成数据库 sequence、专门的计数表或带事务锁的编号服务，避免两个并发请求生成相同编号。

销售时可以展示编号风格，但不要把当前计数方式描述成已经完成高并发生产验证。

---

## 十、订单状态是怎样工作的

当前状态顺序：

```text
Submitted
  → Received
  → Processing
  → Sequenced
  → QC
  → Completed
```

含义：

- **Submitted**：客户已经提交；
- **Received**：实验室确认收到样本；
- **Processing**：样本正在处理；
- **Sequenced**：仪器测序已经完成；
- **QC**：正在进行质量检查；
- **Completed**：订单完成，结果可以交付。

管理员可以从选择框更新状态。系统会同时更新订单当前状态，并增加一条 StatusHistory。

客户订单详情页把六个阶段显示为进度轨迹，因此客户不需要通过电话或邮件反复询问“我的样本到哪一步了”。

这是非常重要的销售价值：状态透明不仅改善客户体验，也减少运营人员处理重复询问的时间。

---

## 十一、结果文件是怎样上传和下载的

### 11.1 上传流程

1. 管理员打开订单；
2. 选择本地结果文件；
3. 浏览器把文件发送到受保护的管理员 API；
4. 服务器确认当前用户是 admin；
5. 服务器确认订单存在；
6. 服务器检查文件不为空且不超过 25 MB；
7. 文件获得随机内部名称；
8. 文件写入 `storage/results/`；
9. 文件资料写入 Result 表；
10. 订单状态自动变为 Completed；
11. 完成状态写入历史。

### 11.2 下载流程

1. 客户点击 Download result；
2. 请求进入 `/api/results/[id]`；
3. 服务器读取登录会话；
4. 服务器查询结果对应订单；
5. 系统确认登录用户是订单所有者，或者是管理员；
6. 验证通过后读取本地文件；
7. 以附件方式返回浏览器；
8. 返回头包含 `private, no-store`，避免把客户结果当成公共缓存内容。

结果目录不位于 Next.js 的公开 `public/` 目录下，因此知道随机文件名也不能绕过 API 直接从静态网址下载。

### 11.3 当前限制

Demo 目前：

- 一个订单一个结果记录；
- 文件保存在本地磁盘；
- 没有病毒扫描；
- 没有对象存储备份；
- 没有自动解析 AB1；
- 没有把单个结果匹配到单个样本；
- 没有自动邮件通知。

生产阶段可以使用 S3 类对象存储、短期签名下载链接、病毒扫描、文件校验和、多个结果记录以及自动生命周期策略。

---

## 十二、安全设计：已经做了什么

### 12.1 密码不是明文保存

密码交给 Better Auth 处理，并使用 scrypt 哈希。系统比较的是密码哈希，而不是读取原始密码。

### 12.2 角色字段不能由注册用户控制

`role` 的认证配置设置了 `input: false`。普通人即使修改前端请求，也不能在注册时要求系统把自己创建为 admin。

### 12.3 页面和 API 都检查权限

客户布局调用 `requireCustomer()`，管理员布局调用 `requireAdmin()`。

更重要的是，创建订单、修改状态、上传结果和下载结果的 API 也会独立检查会话和角色。不能因为有人知道 API 地址就跳过权限。

### 12.4 客户订单使用所有者过滤

客户订单详情使用：

```text
订单 ID + 当前用户 ID
```

同时查询。如果 Alex 把 Maya 的订单 ID 放进网址，服务器不会返回 Maya 的订单，而是返回 404。这个行为已经通过浏览器测试。

### 12.5 上传文件不公开暴露

结果文件：

- 不在公开网页目录；
- 使用随机内部文件名；
- 原始文件名去除换行和引号；
- 受 25 MB 大小限制；
- 下载前执行订单所有权检查。

### 12.6 服务器端输入验证

订单数据在写数据库前使用 Zod 验证，不能只依赖浏览器。

### 12.7 环境密钥不提交到 Git

`.env` 包含本地认证密钥和数据库地址，并被 `.gitignore` 排除。项目只提交 `.env.example` 作为配置模板。

### 12.8 仍需在生产阶段完成的安全工作

这是销售时必须诚实说明的部分：

- 邮箱验证和密码重置；
- 多因素认证；
- 管理员账户更严格的控制；
- 审计日志和操作人记录；
- 登录和接口限流策略验证；
- 文件类型白名单和恶意文件扫描；
- 安全响应头和内容安全策略；
- 依赖持续更新；
- 自动备份和恢复演练；
- 加密、密钥轮换和生产 secrets 管理；
- 第三方渗透测试；
- 隐私、合规和数据保留政策；
- 生产监控、报警和事件响应。

所以正确说法是“原型已经建立了合理的安全边界”，不是“系统已经完成企业级安全认证”。

---

## 十三、主要页面与对应价值

| 页面 | 功能 | 销售时强调的价值 |
| --- | --- | --- |
| 公共首页 | 解释入口和工作流 | 新品牌、新体验，不再依赖旧界面 |
| 注册页 | 收集姓名、邮箱、机构、实验室、电话 | 客户资料从第一步结构化 |
| 登录页 | 客户和管理员进入系统 | 一个统一且清晰的入口 |
| 客户仪表盘 | 显示订单总数、处理中和已完成 | 客户打开页面就知道整体情况 |
| 新 Sanger 订单 | 添加订单信息和多个样本 | 降低实验室填写和重复沟通成本 |
| 订单历史 | 查看全部订单和结果状态 | 形成长期可追踪客户记录 |
| 客户订单详情 | 样本表、状态轨迹、结果下载 | 从提交到结果形成闭环 |
| 管理员仪表盘 | 查看所有新订单和处理队列 | 统一运营入口，减少散落邮件和表格 |
| 管理员订单详情 | 查看客户、样本、PO 和说明 | 实验室得到结构化交接信息 |
| 状态与结果控件 | 更新阶段、上传文件 | 将实验室动作直接同步给客户 |

---

## 十四、系统 API 一览

| 方法和地址 | 作用 | 权限 |
| --- | --- | --- |
| `POST /api/auth/sign-up/email` | 注册客户 | 公共，但受认证规则约束 |
| `POST /api/auth/sign-in/email` | 登录 | 公共 |
| `POST /api/auth/sign-out` | 登出 | 已登录用户 |
| `POST /api/orders` | 创建订单和样本 | 客户 |
| `PATCH /api/admin/orders/[id]/status` | 更新订单状态 | 管理员 |
| `POST /api/admin/orders/[id]/result` | 上传结果并完成订单 | 管理员 |
| `GET /api/results/[id]` | 下载结果 | 订单所有者或管理员 |

页面中的管理员按钮并不是直接修改浏览器里的文字，而是请求 API；API 更新 SQLite；页面重新读取数据库后显示最新内容。

---

## 十五、如何在本地运行 Demo

### 15.1 环境准备

推荐 Node.js 22 LTS。

项目根目录包含 `.nvmrc`，使用 nvm 时可以运行：

```bash
nvm use
```

### 15.2 第一次安装

```bash
npm ci
npm run setup
npm run dev
```

上述命令支持 Windows PowerShell、macOS 和 Linux。`setup` 会在 `.env` 不存在时生成独立的随机登录密钥，创建本地 SQLite 数据库、应用已有迁移并建立两个演示账号。已有 `.env` 会保留；详细的 PC 接续开发流程见 [PC_HANDOFF_ZH.md](PC_HANDOFF_ZH.md)。

然后打开：

```text
http://localhost:3000
```

### 15.3 演示账户

客户：

```text
Email: scientist@demo.local
Password: DemoCustomer!2026
```

管理员：

```text
Email: admin@seqforge.local
Password: SeqForgeDemo!2026
```

这些密码只用于本地 Demo，不能用于生产环境。

### 15.4 常用命令

```bash
npm run dev
```

启动本地开发服务器。

```bash
npm run db:seed
```

创建所需的演示账户。

```bash
npm run db:studio
```

用 Prisma Studio 图形化查看本地数据库。

```bash
npm run verify
```

依次运行 ESLint、TypeScript 和 production build 验证。

---

## 十六、建议的现场演示脚本

## 16.1 三十秒开场

> 这是 SeqForge 新订购平台的第一条完整业务流程。它不是静态网页，而是一个真正保存数据的本地应用。客户可以注册、创建包含多个样本的 Sanger 订单；SeqForge 管理员会立即在运营队列中看到它，更新实验室状态并上传结果；客户随后能看到完成状态并下载自己的文件。这个 Demo 的目的，是证明我们可以在不依赖旧 Retrogen 系统架构的情况下，建立新的客户和实验室工作流。

## 16.2 五分钟演示顺序

### 第一幕：客户入口

1. 打开首页；
2. 强调这是 SeqForge 品牌下的新订购入口；
3. 点击注册或使用演示客户登录；
4. 展示客户仪表盘。

建议讲法：

> 客户不需要面对复杂的内部实验室系统。他们首先看到的是自己的订单总数、处理中订单和已完成结果。

### 第二幕：创建订单

1. 点击 New Sanger order；
2. 输入订单参考名和 PO；
3. 添加第一个样本；
4. 点击 Add another sample；
5. 输入第二个样本和不同引物；
6. 提交订单；
7. 展示自动生成的 SF 订单编号。

建议讲法：

> 一个订单可以包含多条 sequencing reaction。每条记录都带有样本名、模板、浓度、引物来源和备注，所以实验室收到的是结构化数据，不是需要重新整理的自由格式邮件。

### 第三幕：管理员处理

1. 退出客户账户；
2. 登录管理员；
3. 展示新订单出现在队列；
4. 打开订单，展示客户、机构、实验室、样本和 PO；
5. 把状态改成 Processing；
6. 展示状态历史。

建议讲法：

> 客户系统和实验室运营不是两套断开的页面。管理员的状态操作会更新同一份订单数据，客户回来后会看到一致的信息。

### 第四幕：结果交付

1. 上传项目中的 `demo-result.txt`；
2. 展示订单自动变成 Completed；
3. 退出管理员；
4. 再次登录客户；
5. 展示 Completed 标签；
6. 打开订单并下载结果。

建议讲法：

> 结果交付不是把文件发到一个可能找不到的邮件线程里，而是放回对应订单。客户可以回到历史订单中找到它，系统同时验证结果属于哪个客户。

## 16.3 结束语

> 这条流程已经证明 V2 的核心方向可行。下一阶段不是重新推倒，而是在这个基础上增加批量导入、96 孔板、客户价格、其他服务和实验室自动化。今天看到的是产品骨架，也是以后连接 ABI 3730 和轻量 LIMS 的入口。

---

## 十七、从不同对象的角度怎样讲价值

### 17.1 对客户讲

客户最在意的是：

- 下单是否简单；
- 样本有没有被收到；
- 现在处理到哪里；
- 结果在哪里；
- 历史订单能不能找回来。

不要对客户先讲 Next.js、Prisma 或 ORM。应该说：

> 你可以在一个账户里提交样本、查看进度并下载结果，不需要在多个邮件线程里寻找订单信息。

### 17.2 对实验室运营讲

运营人员最在意的是：

- 信息是否完整；
- 每个样本对应哪个引物；
- 特别要求是否清楚；
- 订单队列是否统一；
- 状态是否可以快速更新；
- 结果会不会发错客户。

建议说：

> 每张订单从提交开始就是结构化记录。客户、样本、引物、PO、备注、状态和结果都围绕同一个订单组织，减少人工复制和跨系统查找。

### 17.3 对管理层讲

管理层最在意的是：

- 是否摆脱旧系统风险；
- 是否有可扩展的产品基础；
- 是否改善品牌；
- 是否减少人工沟通；
- 未来能否连接仪器和 LIMS；
- 投入是否可控。

建议说：

> 我们不是继续为旧系统延长寿命，而是已经建立了新的订单、身份、状态和结果数据链。第一阶段成本低、范围可控，同时为后续价格、板式订单和自动结果回传留下扩展路径。

### 17.4 对开发或合作伙伴讲

可以使用更技术化的表达：

> 这是一个基于 Next.js App Router、TypeScript、Prisma 和 Better Auth 的全栈 vertical slice。当前使用 SQLite 进行本地持久化，领域模型包含 User、Order、Sample、Result 和 StatusHistory，API 在服务器执行基于 session、role 和 resource ownership 的授权检查。

---

## 十八、可以推销的核心卖点

### 卖点一：不是旧系统换皮

V2 是新架构和新数据模型，不需要盲目复制 Retrogen 的旧代码。

### 卖点二：客户与实验室共享同一条订单事实

客户看到的状态来自管理员更新的同一条数据库记录，减少信息不一致。

### 卖点三：从样本到结果形成闭环

订单不是在提交后消失。样本、状态、结果和下载都留在订单历史里。

### 卖点四：权限从第一版就进入架构

客户、管理员和订单所有权不是以后再补的界面规则，而是已经进入服务器端流程。

### 卖点五：为扩展服务留出空间

Order 具有通用 service 字段，未来可以扩展其他服务，而不是把整个应用锁死在 Sanger 上。

### 卖点六：本地 Demo 几乎没有外部成本

SQLite、本地文件存储和开源工具让原型不需要付费云服务。这样可以先验证流程，再决定生产基础设施。

### 卖点七：可以逐步连接实验室自动化

未来可以从 Order 和 Sample 生成实验室 ID，加入 plate mapping，再把 ABI 结果匹配回订单，形成轻量 LIMS。

---

## 十九、常见问题与建议回答

### 问：这个现在可以直接上线给客户吗？

答：

> 还不应该直接作为生产系统上线。当前是已经贯通真实数据流程的本地原型，下一步还需要生产数据库、备份、监控、邮件、完整安全测试、审计日志、隐私政策和实验室流程验证。

### 问：为什么不用修旧系统？

答：

> 修旧系统只能暂时缓解问题，而且会继续受旧架构和技术债务限制。V2 先重建最重要的业务闭环，让后续投入都积累在未来平台上。

### 问：为什么数据库现在是 SQLite？

答：

> 因为现在的目标是低成本本地验证。SQLite 可以真实保存数据，又不需要服务器。生产阶段计划使用更适合并发和运维的 PostgreSQL。

### 问：它能处理 96 孔板吗？

答：

> 当前 Demo 支持动态添加多个样本，但还没有专门的 96 孔板界面和 CSV/Excel 导入。那是下一阶段最有价值的扩展之一。

### 问：它能自动连接 ABI 3730 吗？

答：

> 当前结果由管理员手动上传。订单、样本和结果已经有了可关联的数据结构，后续可以增加 bridge computer 或 LIMS 集成，实现 AB1 自动回传和匹配。

### 问：结果安全吗？

答：

> 原型已经在服务器检查用户是否为订单所有者，文件也不放在公开目录。但是正式上线前仍然需要对象存储、备份、病毒扫描、审计、密钥管理和第三方安全测试。

### 问：能不能为不同客户设置不同价格？

答：

> 当前 Demo 故意没有复杂定价。生产路线中可以增加 Organization、Lab、Service 和 PricingRule 的关系，以支持合同价、按反应价和板价。

### 问：未来增加其他服务会不会重写？

答：

> 核心账户、订单状态、结果和权限可以继续复用。不同服务需要增加各自的订单字段和工作流，但不需要重新发明注册、登录、历史订单和结果交付。

---

## 二十、不能过度承诺的地方

销售 Demo 最重要的专业性之一，是准确表达成熟度。

| 已经实现 | 尚未实现 |
| --- | --- |
| 本地真实注册登录 | 生产邮箱验证和找回密码 |
| 客户与管理员角色 | 复杂组织成员和多级权限 |
| 多样本 Sanger 订单 | 96 孔板专用录入和批量导入 |
| SQLite 持久化 | 生产 PostgreSQL 和高可用 |
| 订单状态和历史 | 完整操作人审计和 SLA |
| 手动结果上传下载 | ABI 自动接入和结果自动匹配 |
| 订单所有权隔离 | 正式安全审计和渗透测试 |
| 基础响应式界面 | 全设备和无障碍认证 |
| 本地零云成本 | 生产运维、备份和监控成本 |

一句非常安全且有力量的说法是：

> 这不是最终产品，但它已经证明最终产品最关键的业务链路可以在新架构上成立。

---

## 二十一、从 Demo 到生产的建议路线

### 阶段 A：当前功能性 Demo

- 注册和登录；
- Sanger 多样本订单；
- 管理员队列；
- 状态更新；
- 手动结果上传和下载。

当前状态：已完成并通过本地流程验证。

### 阶段 B：内部演示完善

- 提升大量样本录入效率；
- 增加复制行和批量粘贴；
- 增加自动化端到端测试；
- 优化错误提示；
- 使用真实品牌 Logo；
- 与实验室人员核对字段和状态定义。

### 阶段 C：内部试运行

- PostgreSQL；
- 安全的对象文件存储；
- 邮件通知；
- 审计日志；
- 管理员用户管理；
- 数据备份；
- 生产监控；
- 使用非敏感内部测试订单运行。

### 阶段 D：客户 Beta

- 受控邀请少量客户；
- 邮箱验证和密码重置；
- 服务条款和隐私政策；
- 支持流程；
- 错误和性能监控；
- 迁移少量必要客户资料；
- 严格的权限、安全和恢复测试。

### 阶段 E：生产平台

- 96 孔板和 CSV/Excel 导入；
- Saved primers 和 sample templates；
- 客户特定价格；
- Quotes 和本地取样；
- Oligo、Fragment Analysis 等服务；
- 实验室条码和 plate mapping；
- ABI/LIMS 自动化；
- AB1、FASTA 和 QC 报告自动交付。

---

## 二十二、关键技术词汇中英文对照

| 英文 | 中文解释 |
| --- | --- |
| Frontend | 客户在浏览器中看到和操作的页面 |
| Backend | 服务器上的业务逻辑、权限和数据处理 |
| Full-stack | 同时包含前端和后端 |
| Framework | 建立应用所使用的基础框架 |
| Route | 页面或 API 的网址路径 |
| API | 页面与服务器交换数据的接口 |
| Database | 结构化保存数据的数据库 |
| ORM | 用编程语言安全读写关系数据库的工具 |
| Schema | 数据表、字段和关系的定义 |
| Migration | 数据库结构变化记录 |
| Seed | 自动创建演示或初始数据 |
| Authentication | 确认用户是谁，例如登录 |
| Authorization | 确认用户有权做什么 |
| Session | 登录后的有效会话 |
| Token | 标识登录会话或验证请求的随机值 |
| Password hash | 不可直接还原为原密码的密码表示 |
| Server-side validation | 在服务器再次检查输入 |
| Role-based access | 根据 customer/admin 角色控制权限 |
| Resource ownership | 检查订单是否属于当前客户 |
| Persistence | 页面或服务器重启后数据仍然存在 |
| Responsive design | 页面适应不同屏幕尺寸 |
| Production build | 按正式运行方式编译项目 |
| Dependency audit | 检查第三方软件包已知安全问题 |
| Vertical slice | 从页面到数据库贯通的一条完整业务流程 |
| LIMS | Laboratory Information Management System，实验室信息管理系统 |

---

## 二十三、学习完成后的自我检查

如果你可以不用看文档回答下面的问题，就已经可以比较专业地介绍这个 Demo：

1. 为什么它不是静态 mockup？
2. 客户提交订单后，数据保存在哪里？
3. Next.js、React、Prisma、SQLite 和 Better Auth 各自负责什么？
4. 为什么客户不能查看别人的订单？
5. 管理员更新状态后，客户为什么能看到同样的状态？
6. 结果文件为什么没有直接放到公开目录？
7. 为什么 Demo 用 SQLite，生产计划用 PostgreSQL？
8. 当前 organization 和 lab 是如何保存的？生产版为什么要拆表？
9. 哪些功能已经完成，哪些仍然只是路线图？
10. 为什么先做 Sanger，而没有同时做全部服务？
11. 如何向客户、实验室和管理层分别描述价值？
12. 为什么不能说这个 Demo 已经 production-ready？

最重要的三个结论：

1. **这个 Demo 已经建立了真实的数据和业务闭环。**
2. **它验证的是新平台方向，而不是假装所有生产工作已经完成。**
3. **它的商业价值在于把客户提交、实验室处理和结果交付连接成一条可追踪的数字流程。**
